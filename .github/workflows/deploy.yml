name: Build and Deploy to Cloud Run

on:
  push:
    branches:
      - main

concurrency:
  group: deploy
  cancel-in-progress: true

env:
  PROJECT_ID: project-e5d4de8a-49cc-439d-b6e
  REGION: asia-east1
  SERVICE: hk-transit-pulse
  IMAGE: asia-east1-docker.pkg.dev/project-e5d4de8a-49cc-439d-b6e/cloud-run-source-deploy/hk-transit-pulse

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: Configure Docker
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Build Docker image
        run: |
          docker build -t ${{ env.IMAGE }}:${{ github.sha }} .
          docker tag ${{ env.IMAGE }}:${{ github.sha }} ${{ env.IMAGE }}:latest

      - name: Push Docker image
        run: |
          docker push ${{ env.IMAGE }}:${{ github.sha }}
          docker push ${{ env.IMAGE }}:latest

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE }} \
            --image ${{ env.IMAGE }}:${{ github.sha }} \
            --region ${{ env.REGION }} \
            --service-account bruin-pipeline@project-e5d4de8a-49cc-439d-b6e.iam.gserviceaccount.com \
            --allow-unauthenticated \
            --min-instances=1 \
            --max-instances=1 \
            --set-env-vars GOOGLE_CLOUD_PROJECT=${{ env.PROJECT_ID }}

      - name: Show service URL
        run: |
          gcloud run services describe ${{ env.SERVICE }} \
            --region ${{ env.REGION }} \
            --format="value(status.url)"

      - name: Deploy producer Cloud Run Job
        run: |
          gcloud run jobs deploy producer-job \
            --image ${{ env.IMAGE }}:${{ github.sha }} \
            --region ${{ env.REGION }} \
            --service-account bruin-pipeline@project-e5d4de8a-49cc-439d-b6e.iam.gserviceaccount.com \
            --command python \
            --args "streaming/producer.py" \
            --set-env-vars "GOOGLE_CLOUD_PROJECT=${{ env.PROJECT_ID }},REDPANDA_BROKERS=${{ secrets.REDPANDA_BROKERS }},REDPANDA_USERNAME=${{ secrets.REDPANDA_USERNAME }},REDPANDA_PASSWORD=${{ secrets.REDPANDA_PASSWORD }}" \
            --max-retries 1 \
            --task-timeout 120

      - name: Deploy consumer Cloud Run Job
        run: |
          gcloud run jobs deploy consumer-job \
            --image ${{ env.IMAGE }}:${{ github.sha }} \
            --region ${{ env.REGION }} \
            --service-account bruin-pipeline@project-e5d4de8a-49cc-439d-b6e.iam.gserviceaccount.com \
            --command python \
            --args "streaming/consumer.py" \
            --set-env-vars "GOOGLE_CLOUD_PROJECT=${{ env.PROJECT_ID }},REDPANDA_BROKERS=${{ secrets.REDPANDA_BROKERS }},REDPANDA_USERNAME=${{ secrets.REDPANDA_USERNAME }},REDPANDA_PASSWORD=${{ secrets.REDPANDA_PASSWORD }}" \
            --max-retries 1 \
            --task-timeout 120
